version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8080"
    env_file:
      - ./backend/.env
  frontend:
    build: ./frontend
    ports:
      - "3000:8080"
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
